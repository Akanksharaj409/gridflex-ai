PORT=4000
# Optional. Without it the AI assistant uses the deterministic explainer.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Optional. Without it the app runs on the in-memory store.
MONGO_URI=
