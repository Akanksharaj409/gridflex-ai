{
  "name": "gridflex-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Neighbourhood renewable energy reliability platform - forecast, detect shortage, optimize, measure impact",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
